extends /layout

block title
  | #{errorMessage}

block content
  h1 #{errorMessage}
  if process.env.NODE_ENV == 'development'
    h3= params.error.message
    pre
      code= params.error.stack
  else
    p Feel free to improve our error logging... That way we might tell you what went wrong...
